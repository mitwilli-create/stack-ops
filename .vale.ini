# Vale config — prose linting wired to Mitchell's Voice OS rules.
# Run: vale docs/  (or any .md). Install: brew install vale.
StylesPath = styles
MinAlertLevel = suggestion

[*.{md,txt}]
BasedOnStyles = VoiceOS
