{
  "name": "stack-ops",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Automatic, research-validated model-triage system for a multi-vendor AI stack.",
  "scripts": {
    "test": "node --test src/**/*.test.mjs scripts/**/*.test.mjs",
    "gate": "node src/router/cli.mjs",
    "route": "node src/router/route-request.mjs",
    "ask": "node src/router/ask.mjs",
    "console": "node src/agent/server.mjs",
    "check:mcp": "node scripts/check-mcp-config.mjs",
    "check:mcp:selftest": "node scripts/check-mcp-config.mjs --self-test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "mem0ai": "^3.1.0"
  }
}
